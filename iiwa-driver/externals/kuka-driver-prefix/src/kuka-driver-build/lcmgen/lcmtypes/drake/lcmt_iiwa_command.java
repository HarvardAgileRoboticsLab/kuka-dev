/* LCM type definition class file
 * This file was automatically generated by lcm-gen
 * DO NOT MODIFY BY HAND!!!!
 */

package drake;
 
import java.io.*;
import java.util.*;
import lcm.lcm.*;
 
public final class lcmt_iiwa_command implements lcm.lcm.LCMEncodable
{
    public long timestamp;
    public int num_joints;
    public double joint_position[];
    public int num_torques;
    public double joint_torque[];
 
    public lcmt_iiwa_command()
    {
    }
 
    public static final long LCM_FINGERPRINT;
    public static final long LCM_FINGERPRINT_BASE = 0xe0bb6ab386c61eceL;
 
    static {
        LCM_FINGERPRINT = _hashRecursive(new ArrayList<Class<?>>());
    }
 
    public static long _hashRecursive(ArrayList<Class<?>> classes)
    {
        if (classes.contains(drake.lcmt_iiwa_command.class))
            return 0L;
 
        classes.add(drake.lcmt_iiwa_command.class);
        long hash = LCM_FINGERPRINT_BASE
            ;
        classes.remove(classes.size() - 1);
        return (hash<<1) + ((hash>>63)&1);
    }
 
    public void encode(DataOutput outs) throws IOException
    {
        outs.writeLong(LCM_FINGERPRINT);
        _encodeRecursive(outs);
    }
 
    public void _encodeRecursive(DataOutput outs) throws IOException
    {
        outs.writeLong(this.timestamp); 
 
        outs.writeInt(this.num_joints); 
 
        for (int a = 0; a < this.num_joints; a++) {
            outs.writeDouble(this.joint_position[a]); 
        }
 
        outs.writeInt(this.num_torques); 
 
        for (int a = 0; a < this.num_torques; a++) {
            outs.writeDouble(this.joint_torque[a]); 
        }
 
    }
 
    public lcmt_iiwa_command(byte[] data) throws IOException
    {
        this(new LCMDataInputStream(data));
    }
 
    public lcmt_iiwa_command(DataInput ins) throws IOException
    {
        if (ins.readLong() != LCM_FINGERPRINT)
            throw new IOException("LCM Decode error: bad fingerprint");
 
        _decodeRecursive(ins);
    }
 
    public static drake.lcmt_iiwa_command _decodeRecursiveFactory(DataInput ins) throws IOException
    {
        drake.lcmt_iiwa_command o = new drake.lcmt_iiwa_command();
        o._decodeRecursive(ins);
        return o;
    }
 
    public void _decodeRecursive(DataInput ins) throws IOException
    {
        this.timestamp = ins.readLong();
 
        this.num_joints = ins.readInt();
 
        this.joint_position = new double[(int) num_joints];
        for (int a = 0; a < this.num_joints; a++) {
            this.joint_position[a] = ins.readDouble();
        }
 
        this.num_torques = ins.readInt();
 
        this.joint_torque = new double[(int) num_torques];
        for (int a = 0; a < this.num_torques; a++) {
            this.joint_torque[a] = ins.readDouble();
        }
 
    }
 
    public drake.lcmt_iiwa_command copy()
    {
        drake.lcmt_iiwa_command outobj = new drake.lcmt_iiwa_command();
        outobj.timestamp = this.timestamp;
 
        outobj.num_joints = this.num_joints;
 
        outobj.joint_position = new double[(int) num_joints];
        if (this.num_joints > 0)
            System.arraycopy(this.joint_position, 0, outobj.joint_position, 0, this.num_joints); 
        outobj.num_torques = this.num_torques;
 
        outobj.joint_torque = new double[(int) num_torques];
        if (this.num_torques > 0)
            System.arraycopy(this.joint_torque, 0, outobj.joint_torque, 0, this.num_torques); 
        return outobj;
    }
 
}

